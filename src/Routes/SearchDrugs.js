import { Fragment } from 'react';
import SearchBar from "../Components/SearchBar";

const SearchDrugs = () => {
    return (
        <Fragment>
            <SearchBar />
        </Fragment>
    );
}

export default SearchDrugs;
